{
  "name": "tactical-skirmish-electron",
  "version": "1.0.0",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "build": "cd frontend && npm install && npm run build"
  },
  "devDependencies": {
    "electron": "^29.0.0"
  }
}
